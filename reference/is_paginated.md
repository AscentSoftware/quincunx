# Is the response paginated?

Checks if the response is paginated by checking if a count element
exists in the response.

## Usage

``` r
is_paginated(json_string)
```

## Arguments

- json_string:

  a string.

## Value

A logical value.
